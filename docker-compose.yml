version: '3.8'

services:
  web:
    build: .
    ports:
      - "3000:3000"  # Next.js
      - "5000:5000"  # Flask image finder
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    env_file:
      - .env.docker
    command: npm run dev
